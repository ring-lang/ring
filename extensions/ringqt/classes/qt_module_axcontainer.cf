<comment>
				Module (axcontainer)
</comment>

<filter> (iswindows() and sysget("RINGQT_ANDROID") = "")
<loadfile> qaxbase.cf
<loadfile> qaxobject.cf
<loadfile> qaxwidget.cf
<loadfile> qaxwidget2.cf
</filter>
