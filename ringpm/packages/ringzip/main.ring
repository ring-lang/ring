func main
	C_LINESIZE = 80
	? copy("=",C_LINESIZE)
	? "RingZip Package"
	? copy("=",C_LINESIZE)
	? "RingZip extension for the Ring programming language"
	? "See the folder : ring/extensions/ringzip"
	? copy("=",C_LINESIZE)