/*
	Application	: Convesion between Lists & Strings
	Author		: Khaled ABID
	Date		:25/12/2017
*/

# Variable Types (String, Number, List, Object, C Object)

# 	 1	2	3	4	5
aList = [:one, :two, :three, :four, :five]

? aList ? len(aList)

cString = list2str(aList) ? len(cString) ? cString

aList2 = str2list(cString) ? len(aList2) ? aList
