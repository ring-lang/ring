func main
	C_LINESIZE = 80
	? copy("=",C_LINESIZE)
	? "RingFmt Package"
	? copy("=",C_LINESIZE)
	? "RingFmt package for the Ring programming language"
	? "See the folder : ring/tools/ringfmt"
	? "To run the application, run : ringfmt"
	? copy("=",C_LINESIZE)