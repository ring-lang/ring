func main
	C_LINESIZE = 80
	? copy("=",C_LINESIZE)
	? "RingThreads Package"
	? copy("=",C_LINESIZE)
	? "RingThreads extension for the Ring programming language"
	? "See the folder : ring/extensions/ringthreads"
	? copy("=",C_LINESIZE)