func main
	C_LINESIZE = 80
	? copy("=",C_LINESIZE)
	? "BigNumber Package"
	? copy("=",C_LINESIZE)
	? "BigNumber package for the Ring programming language"
	? "See the folder : ring/ringlibs/bignumber"
	? copy("=",C_LINESIZE)