# بسم الله الرحمن الرحيم
// this is Lesson6 -  thanks to Mr.MAHMOUD FAYED
/*
  Application:  Functions  
  Author     :  S.BOURROU
  Date       :  17.12.2017  
*/
x  = 10  # Global variable $x
$a = "$$$"
def main 
        ? "This is my main function !"
       one() # 10
       myfunc()
def one 
        ? x   

# in ring i can replace func with  def !

func myfunc {
        ? "an other style !"
        ? $a
}
