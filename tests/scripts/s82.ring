aList = [1,2,3]

for x = 4 to 10 add(aList,x) next

For x = 1 to len(aList) See aList[x] See NL Next

mylist = []
 
for x = 1 to 10 add(mylist,[x,x*x]) next

for x = 1 to 10
	see "Number : " + mylist[x][1] + "  Square : " + mylist[x][2] + nl
next
