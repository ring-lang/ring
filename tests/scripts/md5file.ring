cStr = read("B:\Mahmoud\doublesvsoop\PWCT Project\First Generation\Environment\Setup\NewSetup\pwct_NSIS\Fayed_PWCT_1.9_Art.exe")
see "Size : " + len(cStr) + nl
see "md5 : " + md5(cStr) + nl
see "sha1 : " + sha1(cStr) + nl
see "sha256 : " + sha256(cStr) + nl
see "sha224 : " + sha224(cStr) + nl
see "sha384 : " + sha384(cStr) + nl
see "sha512 : " + sha512(cStr) + nl
