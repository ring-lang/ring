
see " command from sub" + NL

load "tests\s56.nova"

see " command from sub" + NL
see " command from sub" + NL
see " command from sub" + NL

func sub
	see "wow i am here" + NL
	see "NICE" + NL
	see "sub() file name : " + filename() + nl
	#return