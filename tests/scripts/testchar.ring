cStr = "mahmoud123_"
see isalnum(cStr) + nl
cStr = "mahmoud123"
see isalnum(cStr) + nl
cStr = "mahmoud"
see isalpha(cStr) + nl
cStr = "mahmoud123"
see isalpha(cStr) + nl
cStr = "123"
see isdigit(cStr) + nl
cStr = "123a"
see isdigit(cStr) + nl
cStr = "FFA10"
see isxdigit(cStr) + nl
cStr = "FFA10i"
see isxdigit(cStr) + nl
cStr = "UPPER"
see isupper(cStr) + nl
cStr = "lower"
see isupper(cStr) + nl
cStr = "UPPER"
see islower(cStr) + nl
cStr = "lower"
see islower(cStr) + nl
cStr = "       "
see isspace(cStr) + nl
cStr = "     s  "
see isspace(cStr) + nl
cStr = "abcdefg "
see isprint(cStr) + nl
see isgraph(cStr) + nl
cStr = "abcdefg"
see isgraph(cStr) + nl
see iscntrl(cStr) + nl
cStr = char(9)
see iscntrl(cStr) + nl

