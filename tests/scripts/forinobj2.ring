new test {
	for g=1 to 100
	point {}
	point {}
	next
	
}

class test
	aList = []
	

	func add oObject
		aList + oObject

	func getpoint
		add( new point )

	func braceend
		main()

	Func main

	while true
		#for r in alist r.hello()  next
		for t in alist t.hello()  next
		for t in alist t.hello()  see t.x  next
		callgc()
		see alist
	end

	private
		point

class point 
	x=10 y=20 z=30
	func hello 