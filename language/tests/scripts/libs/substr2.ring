cStr = "test"

? "Using ? substr(cStr,100, 1)"
? substr(cStr,100, 1)

? "Using ? substr(cStr, -1, 4)"
? substr(cStr, -1, 4)

? "Using ? substr(cStr, 1, 1000)"
? substr(cStr, 1, 1000)

? "Using ? substr(cStr, 1, -1)"
? substr(cStr, 1, -1)

? :done