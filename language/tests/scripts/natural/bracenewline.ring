new myclass {
	i want window
	window title is "hello world"
}

? copy("=",20)

new myclass { i want window }

class myclass

	func braceStart       ? :welcome
	func braceError
	func braceEnd         ? :bye
	func braceNewLine     ? :newline

	
