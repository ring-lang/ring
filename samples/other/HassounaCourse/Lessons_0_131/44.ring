
text = "Welcome To Egypt"
text = "AHMED"

newText = ""

for x=len(text) to 1 step -1
	newText += text[x]
next


/*
for x=len(text) to 1 step -1
	if text[x]!="e"
		newText += text[x]
	ok
next
*/


see newText

/*
for x=1 to len(text)
	newText += text[x]
next

see newText
*/
