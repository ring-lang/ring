/*
for x=1 to 5

	for y=1 to 3
		see "x=" + x + " ; y=" + y + nl
	next	

next
*/

/*
for c=1 to 5

	see "Class " + c + ":" + nl

	for s=1 to 3
		see "   Student " + s + nl
	next

	see "===============" + nl

next
*/



for basenum = 1 to 10

	see "Table " + basenum + " :" + nl

	for num = basenum to 10
		see "   " + basenum + " * " + num + " = " + (basenum*num) + nl
	next

	see "=======================" + nl

next


