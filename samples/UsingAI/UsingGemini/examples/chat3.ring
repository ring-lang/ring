#================================================#
# This code is generated by Gemini (AI)
# Questions by Fayed
#================================================#

// Loop through numbers 1 to 20
for i = 1 to 20

  // Calculate square of the number
  square = i * i

  // Define description based on the number
  desc = "even"
  if (i % 2) != 0
    desc = "odd"
  end

  // Print the number, square, and description
  ? "" + i + " squared is " + square + ". It's an " + desc + " number."
next